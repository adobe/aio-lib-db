const DocDBCommand = require('./DocDBCommand');
const { isString } = require('./utils');
const { DocDBLibError } = require('./errors');

class DocDB {
  constructor(options = {}) {
    }

    
}

module.exports = DocDB;
